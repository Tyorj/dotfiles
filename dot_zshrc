# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
# --- Powerlevel10k Instant Prompt (MUST BE AT THE TOP) ---
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

# --- Setup Variables ---
ZCONF="$HOME/.config/zsh"  

# --- Core Modules ---
source "$ZCONF/bindings.zsh" 
source "$ZCONF/options.zsh"     

source "$ZCONF/aliases.zsh"  

# --- Plugins ---
source "/usr/share/wikiman/widgets/widget.zsh"
source "$ZCONF/plugins/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh"
ZSH_HIGHLIGHT_STYLES[suffix-alias]='fg=184,underline'
ZSH_HIGHLIGHT_STYLES[precommand]='fg=184,underline'
ZSH_HIGHLIGHT_STYLES[arg0]='fg=184'
# source "$ZCONF/plugins/zsh-autocomplete/zsh-autocomplete.plugin.zsh"
autoload -U compinit; compinit
_comp_options+=(globdots)

source <(fzf --zsh)
source "$ZCONF/plugins/fzf-tab/fzf-tab.plugin.zsh"
source "$ZCONF/plugins/zsh-autosuggestions/zsh-autosuggestions.zsh"

export AUTOENV_ENABLE_LEAVE=1
source "$ZCONF/plugins/autoenv/activate.sh"

source "$ZCONF/plugins.zsh" 

# --- Theme ---
source "$ZCONF/plugins/powerlevel10kgit/powerlevel10k.zsh-theme"


# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f "$ZCONF/.p10k.zsh" ]] || source "$ZCONF/.p10k.zsh"

# To customize prompt
[[ ! -f ~/.config/zsh/.p10k.zsh ]] || source ~/.config/zsh/.p10k.zsh

eval "$(zoxide init zsh)"
